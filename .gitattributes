# Нормализуем переводы строк: в репозитории LF, при checkout — нативные.
* text=auto eol=lf

# Windows-скрипты держим в CRLF, чтобы корректно запускались.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Бинарные данные не трогаем.
*.xlsx binary
*.pdf  binary
*.png  binary
