# Зависимости для разработки и CI.
# Включают рантайм (requirements.txt) + инструменты тестирования.
#
# For a reproducible development environment use `uv sync --locked`.
# This file remains as a legacy pip entry point.

-r requirements.txt

pytest>=8.0
ruff>=0.15
mypy>=1.10
